kind = "web"
previewPath = "/"
title = "Multimodal Education Creator"
version = "1.0.0"
id = "artifacts/education-creator"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25793

[services.development]
run = "pnpm --filter @workspace/education-creator run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/education-creator", "run", "build" ]
publicDir = "artifacts/education-creator/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25793"
BASE_PATH = "/"
